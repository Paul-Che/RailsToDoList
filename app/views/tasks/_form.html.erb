 <div class="text-center padded">
  <%= form_for(@task) do |t| %>
    <%= t.label :name %>
    <%= t.text_field :name %>

    <%= t.label :content %>
    <%= t.text_field :content %>

    <%= t.submit %>
  <% end %>
</div>
